# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=core
DB_USER=core
DB_PASSWORD=changeme
DB_SCHEMA=public

# JWT (shared secret with identity service — must match)
JWT_SECRET=change-me-to-a-256-bit-secret-at-least-32-chars

# Identity Service
IDENTITY_SERVICE_URL=http://localhost:8080

# CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Observability
LOKI_URL=http://loki:3100
